import 'package:flutter/material.dart';

final BoxDecoration selectedTabStyle = BoxDecoration(
    color: Color(0XFF000E33),
);

final BoxDecoration defaultTabStyle = BoxDecoration(
  color: Color(0XFFADB8C3),
);

final Container defaultContainerStyle = Container(
  margin: const EdgeInsets.only(right: 8.0),
  height: 2.0,
  width: 25.0,
);

final Container selectedContainerStyle = Container(
  margin: const EdgeInsets.only(left: 8.0),
  height: 2.0,
  width: 40.0,
);